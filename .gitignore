# Build outputs (matches any Gradle subproject)
android/**/build/
android/build/

# Gradle caches
android/.gradle/
.gradle/

# Kotlin daemon caches
android/**/.kotlin/
android/.kotlin/

# IDE
.idea/
*.iml
local.properties

# Python
ml/.venv/
ml/__pycache__/
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Pre-install on-device DB snapshots — see `make db-backup`
db-backups/

# Claude Code session artifacts
.claude/worktrees/

# Owner personal lab/medical records — health data stays on-device, never
# pushed to the (public) remote. Generic owner-subject docs (PROFILE,
# VESSEL_WATCH, LAB_PANEL_ORDER) remain tracked; raw result dumps do not.
docs/subjects/owner/LAB_RESULTS_*.md

# Claude Code local config — never ship to auditors
.claude/

# Owner subject data — personal medical, never commit
docs/subjects/owner/
