<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Health Connect permissions -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE" />
    <uses-permission android:name="android.permission.health.READ_SKIN_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_VO2_MAX" />
    <!-- Body composition (Weight + BodyFat + LeanBodyMass) — read paths in
         HealthConnectBodyComposition.kt. Missing-from-manifest causes the
         HC permission contract to filter these out of the requestable set,
         which silently turns onboarding's "Grant Permissions" button into
         a no-op once the other perms are granted. -->
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_BODY_FAT" />
    <uses-permission android:name="android.permission.health.READ_LEAN_BODY_MASS" />
    <!--
      Required so SyncWorker (WorkManager, runs in background) can read data
      written by other apps (Google Fit, Fitbit, etc.). Without this, Health
      Connect silently enforces "only the calling app's own data" on background
      reads and Bios sees empty results because it has never written any data.
    -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />

    <!-- Network (P2P sync, API adapters) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Background work -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Phone-sleep collection foreground service (#241). FOREGROUND_SERVICE
      is the base permission; FOREGROUND_SERVICE_HEALTH is required on
      API 34+ for the documented "sleep / vitals" service category that
      PhoneSleepCollectionService declares. WAKE_LOCK keeps the CPU
      available so the per-minute sampling cadence does not drift under
      Doze while the service is running.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Step counter. Android 10+ silently delivers zero events to
         Sensor.TYPE_STEP_COUNTER without this permission, even though
         the sensor itself reports `hasStepCounter = true`. Without this
         declaration the phone-side step source stays empty forever. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Camera (fingertip-PPG HRV snapshots via rear camera + flash) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <!--
      Bluetooth-LE air-quality peripherals (#43). Owner-initiated pairing in
      Settings → Pair air-quality sensor; runtime data flow only when a device
      is paired. API 31+ uses the split scan/connect permissions; older
      releases keep the legacy ones plus FINE_LOCATION (the OS gates BLE scans
      on location until Android 12).
    -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <!--
      Two-layer gate for companion access (Phase 7, Option C):
        Layer 1 — these "dangerous" permissions; the OS prompts the owner when a
        companion app declares them.
        Layer 2 — Bios's per-package allowlist (Settings → Companion Apps),
        managed by the owner. The OS permission is the coarse filter; the
        allowlist is the fine filter. See provider/CompanionGate.kt.
      Rationale: each companion keeps its own signing key, third-party companions
      are possible, and a single leaked key never compromises the whole suite.
    -->
    <permission
        android:name="com.bios.app.permission.READ_HEALTH"
        android:protectionLevel="dangerous"
        android:label="Read Bios health data"
        android:description="@string/permission_read_health_desc" />
    <permission
        android:name="com.bios.app.permission.WRITE_COMPANION"
        android:protectionLevel="dangerous"
        android:label="Write companion signals to Bios"
        android:description="@string/permission_write_companion_desc" />

    <application
        android:name=".BiosApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bios">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Bios">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Health Connect permissions intent -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>

            <!-- Companion deep link: bios://capture/ppg lands directly on the
                 PPG capture screen so companions (W2F's "take a snapshot" CTA)
                 can offer one-tap entry without a Home detour. See
                 docs/CAMERA_PPG.md § Deep link. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bios" android:host="capture" android:path="/ppg" />
            </intent-filter>

            <meta-data
                android:name="health_permissions"
                android:resource="@array/health_permissions" />
        </activity>

        <!-- Health data provider for companion apps (W2F). Signature-protected. -->
        <provider
            android:name=".provider.BiosHealthProvider"
            android:authorities="com.bios.app.health"
            android:exported="true"
            android:readPermission="com.bios.app.permission.READ_HEALTH"
            android:writePermission="com.bios.app.permission.WRITE_COMPANION" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Declare Health Connect data types used -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
            android:targetActivity=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
        <!-- URGENT-alert notification-action handler (#179). Owner taps
             "I'm OK" → cancels the pending UrgentAckTimeoutWorker so no
             SMS-compose prompt fires. Not exported — only Bios itself
             builds PendingIntents pointing here. -->
        <receiver
            android:name=".alerts.AlertActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.bios.app.alerts.ACTION_ACKNOWLEDGE" />
            </intent-filter>
        </receiver>
        <!-- UnifiedPush service for Google-free push notifications.
             The connector library's internal receiver forwards events here.
             Not exported — only the library binds to it. -->
        <service
            android:name=".push.BiosPushService"
            android:exported="false">
            <intent-filter>
                <action android:name="org.unifiedpush.android.connector.PUSH_EVENT" />
            </intent-filter>
        </service>

        <!-- Phone-sleep collection foreground service (#241 Cut 1).
             Started by PowerConnectionReceiver on ACTION_POWER_CONNECTED
             during quiet-hours; stopped on ACTION_POWER_DISCONNECTED.
             Not exported — only Bios itself starts it. The "health"
             foregroundServiceType is documented for sleep / vitals
             collection on API 34+; older releases ignore the attribute.

             The PowerConnectionReceiver is registered at runtime in
             BiosApplication.onCreate — ACTION_POWER_CONNECTED /
             _DISCONNECTED are NOT on the API 26+ implicit-broadcast
             allowlist, so manifest-declared receivers never fire. -->
        <service
            android:name=".ingest.PhoneSleepCollectionService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Quick Settings tile for owner-asserted bedtime ↔ wake log
             (#242). The tile is exported (the system Settings UI binds
             to it to enumerate available tiles for the picker) and
             gated by the BIND_QUICK_SETTINGS_TILE permission, which is
             only granted to the SystemUI process. -->
        <service
            android:name=".ingest.SleepTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_lock_idle_alarm"
            android:label="Going to bed"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>
</manifest>
